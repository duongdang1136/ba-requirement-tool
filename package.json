{
  "name": "ba-requirement-tool",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix frontend run build",
    "cleanup": "node scripts/cleanup.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
